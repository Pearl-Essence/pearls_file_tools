version: 2
updates:
  - package-ecosystem: "pip" # Tells Dependabot to look for Python packages
    directory: "/"           # Where your requirements.txt is located
    schedule:
      interval: "weekly"     # How often to check for updates (daily, weekly, monthly)
    open-pull-requests-limit: 5 # Prevents your PR tab from getting flooded
